# v0.3.0 Cleanup PR Checklist

> Scope: validate cleanup completion for canonical-only imports and compatibility-removal consistency.

## 1) Canonical-only import enforcement

### Files to review
- `README.md`
- `docs/**/*.md` (especially examples and onboarding docs)
- `tests/**/*.py` that document import paths (`tests/test_api_canonical_paths.py`, `tests/test_docs_canonical_imports.py`)

### Required checks
- [ ] No new docs/examples import from root compatibility shims when canonical `temporal_gradient.*` paths exist.
- [ ] Tests assert canonical import paths as the expected/default public surface.
- [ ] Any remaining temporary compatibility import mention is explicitly marked as migration-only with a removal version.

## 2) Shim/alias removal-state consistency

### Files to review
- `temporal_gradient/clock/chronos.py`
- `docs/MIGRATION_SHIMS.md`
- `docs/CANONICAL_VS_LEGACY.md`
- `TASK_PROPOSALS.md`

### Required checks
- [ ] Code state and docs state agree (no doc claims an alias/shim exists if code removed it, and vice versa).
- [ ] Removal status in `TASK_PROPOSALS.md` matches implementation state (planned vs in-progress vs complete).
- [ ] Deprecation/removal wording includes concrete target patch versions.

### Shim removal targets (per subsystem)
Each root-level shim must be explicitly targeted for a patch version before the v0.3.0 release.

- [ ] **`chronos_engine.py`** (clock shim) — target removal: `v0.3.0`
  - Canonical replacement: `from temporal_gradient.clock.chronos import ClockRateModulator`
- [ ] **`salience_pipeline.py`** (salience shim) — target removal: `v0.3.0`
  - Canonical replacement: `from temporal_gradient.salience.pipeline import SaliencePipeline` (and related symbols)
- [ ] **`entropic_decay.py`** (memory shim) — target removal: `v0.3.0`
  - Canonical replacement: `from temporal_gradient.memory.decay import DecayEngine` (and related symbols)
- [ ] **`chronometric_vector.py`** (telemetry shim) — target removal: `v0.3.0`
  - Canonical replacement: `from temporal_gradient.telemetry.chronometric_vector import ChronometricVector`
- [ ] **`temporal_gradient.telemetry.validate_packet`** (schema alias) — target removal: `v0.3.0`
  - Canonical replacement: `from temporal_gradient.telemetry.schema import validate_packet_schema`

## 3) Required validation commands (same logical gates as CI)

1. [ ] `python -m pytest -q tests/test_packet_contract_check.py`
2. [ ] `pytest -q`
3. [ ] `python scripts/check_docs_consistency.py`
4. [ ] `python -m pytest -q tests/test_api_canonical_paths.py tests/test_docs_canonical_imports.py tests/test_root_shims.py`

## 4) PR readiness

- [ ] PR description links this checklist and marks each section complete.
- [ ] PR summary calls out canonical import changes and any compatibility removals.
- [ ] If removal work is partial, explicitly list follow-up task IDs and target patches.
